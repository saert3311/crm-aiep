<main class="flex-shrink-0">
<%= render "shared/header" %>
    <div class="container">
    <div class="row justify-content-md-center">
        <div class="col-4">
            <div class="jumbotron">
                <h1 class="display-4"><%= @prospect.fname %> <%= @prospect.lname %></h1>
                <p class="lead"><%= @prospect.status %></p>
                <hr class="my-4">
                <p><%= @prospect.tel %></p>
                <p><%= @prospect.email %></p>
                <p class="lead">
                    <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
                </p>
            </div>
        </div>
        <div class="col-4">
        Conversaciones
        </div>
</div>
    </div>
</main>
<%= render "shared/footer" %>